<Window x:Class="DotGame.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DotGame - 2D Physics Particle Simulator"
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <!-- Simulation Canvas -->
        <Border Grid.Column="0" BorderBrush="Black" BorderThickness="2" Margin="10">
            <Canvas x:Name="SimulationCanvas"
                    Width="800"
                    Height="600"
                    Background="WhiteSmoke"/>
        </Border>

        <!-- Control Panel -->
        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="10" Padding="10">
            <StackPanel>
                <TextBlock Text="Particle Physics Simulator"
                          FontSize="16"
                          FontWeight="Bold"
                          Margin="0,0,0,20"/>

                <!-- Control Buttons -->
                <GroupBox Header="Controls" Margin="0,0,0,10">
                    <StackPanel>
                        <Button x:Name="StartButton"
                               Content="Start"
                               Click="StartButton_Click"
                               Margin="0,5"
                               Padding="5"/>
                        <Button x:Name="StopButton"
                               Content="Stop"
                               Click="StopButton_Click"
                               Margin="0,5"
                               Padding="5"/>
                        <Button x:Name="ResetButton"
                               Content="Reset"
                               Click="ResetButton_Click"
                               Margin="0,5"
                               Padding="5"/>
                    </StackPanel>
                </GroupBox>

                <!-- Configuration -->
                <GroupBox Header="Configuration" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Particle Count:"/>
                        <TextBox x:Name="ParticleCountTextBox"
                                Text="50"
                                Margin="0,2,0,10"/>

                        <TextBlock Text="Random Seed:"/>
                        <TextBox x:Name="SeedTextBox"
                                Text="12345"
                                Margin="0,2,0,10"/>

                        <TextBlock Text="Gravity Strength:"/>
                        <TextBox x:Name="GravityTextBox"
                                Text="100.0"
                                Margin="0,2,0,10"/>
                    </StackPanel>
                </GroupBox>

                <!-- Physics Toggles -->
                <GroupBox Header="Physics" Margin="0,0,0,10">
                    <StackPanel>
                        <CheckBox x:Name="UseGravityCheckBox"
                                 Content="Gravity"
                                 IsChecked="True"
                                 Margin="0,5"/>
                        <CheckBox x:Name="UseCollisionsCheckBox"
                                 Content="Collisions"
                                 IsChecked="True"
                                 Margin="0,5"/>
                        <CheckBox x:Name="UseBoundariesCheckBox"
                                 Content="Boundaries"
                                 IsChecked="True"
                                 Margin="0,5"/>
                        <CheckBox x:Name="UseDampingCheckBox"
                                 Content="Damping"
                                 IsChecked="True"
                                 Margin="0,5"/>
                    </StackPanel>
                </GroupBox>

                <!-- Info -->
                <GroupBox Header="Info">
                    <StackPanel>
                        <TextBlock x:Name="InfoTextBlock"
                                  Text="Click Start to begin"
                                  TextWrapping="Wrap"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </Border>
    </Grid>
</Window>
